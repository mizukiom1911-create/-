# Shopifyへの反映手順（フェーズ1）

このフォルダを、みずきさんのShopifyストアに反映するための手順書です。
**フェーズ1では、麦茶LPの「上半分（ヒーロー部分まで）」+ ヘッダー・フッター・アナウンスバー**が動く状態になります。

---

## 全体の流れ

```
0. まず preview-phase1.html を確認（見た目のズレがないか）
1. テーマを複製（バックアップ）
2. Shopify商品「純手炒り むぎ茶」を作成（バリアント登録）
3. assets/theme.css をアップロード
4. layout/theme.liquid に読み込み1行を追加
5. sections/ ×4ファイルをアップロード
6. snippets/badge-shubun.liquid をアップロード
7. templates/page.mugicha.json をアップロード
8. ページを作成し、テンプレートに割り当て
9. プレビューで確認
```

**所要時間の目安：30〜60分**

---

## ステップ 0：まず preview-full.html を確認

このプロジェクト内の `shopify-integration/preview-full.html` を開いて、
**見た目に違和感がないか**、まず確認してください。
（フェーズ1だけ確認したい場合は `preview-phase1.html` も残してあります）

- 色味（紺・生成り・金）が意図通りか
- 明朝の重さがちょうどよいか
- バリアント切替・数量ボタン・カート追加のインタラクションがイメージ通りか

**違和感があれば、まずここで直す方が後戻りが少ないです。**

---

## ステップ 1：テーマを複製（必ずやる）

1. Shopify管理画面 → **オンラインストア** → **テーマ**
2. 現在のテーマの **「…」（三点メニュー）** → **「複製」**
3. 複製したテーマの名前を **「nagaoka-mugicha-v1」** などに変更
4. これから編集するのは **複製した方**

複製テーマは公開しない限り、みずきさんしか見えません。安心して壊してOK。

---

## ステップ 2：Shopifyで麦茶商品を作成

麦茶ヒーローは **Shopifyの商品** と連動します（Ajaxカート追加のため）。

1. **商品管理** → **商品を追加**
2. 商品名：`純手炒り むぎ茶（上州屋設楽彦吉商店）`
3. **バリアント** を追加：
   - オプション名：`タイプ`
   - オプションの値：`丸粒 330g` / `丸粒 150g` / `ティーパック 15包` / `ティーパック 5包` / `ティーパック 1包`
4. それぞれのバリアントに **価格・在庫・SKU** を設定
5. 商品ステータスを **「有効」** に

> 💡 5つ全部を最初から入れる必要はありません。フェーズ1では**丸粒2種類だけ**でOK。

---

## ステップ 3：theme.css をアップロード

1. 複製テーマの **「…」** → **「コードを編集」**
2. 左サイドバー **「Assets」** → **「新しいアセットを追加」** → **「ファイルをアップロード」**
3. このプロジェクトの `shopify-integration/assets/theme.css` をアップロード

---

## ステップ 4：theme.liquid に読み込み1行を追加

1. 左サイドバー **「Layout」** → **「theme.liquid」** を開く
2. `</head>` タグを検索（Ctrl+F / Cmd+F）
3. `</head>` の**直前**に：

```liquid
{{ 'theme.css' | asset_url | stylesheet_tag }}
```

4. **保存**

---

## ステップ 5：sections/ 内のLiquidファイルをアップロード

**「Sections」** フォルダ → **「新しいセクションを追加」** で、以下4つを追加。
アップロード時は **ファイル内容をコピペ** します（`.liquid` は直接アップロードできないため、名前をつけて中身を貼り付ける形）。

| ローカルファイル | Shopifyでのファイル名 |
|---|---|
| `sections/announcement-bar.liquid` | `announcement-bar` |
| `sections/header-nagaoka.liquid` | `header-nagaoka` |
| `sections/footer-nagaoka.liquid` | `footer-nagaoka` |
| `sections/mugicha-hero.liquid` | `mugicha-hero` |

**手順（各ファイルごとに繰り返し）：**
1. Sections フォルダ → 「新しいセクションを追加」
2. 名前を入力（上表の右列。拡張子は自動で付く）
3. デフォルトの中身を消して、ローカルファイルの中身を全部コピペ
4. 保存

---

## ステップ 6：snippets/ をアップロード

同じ要領で、`snippets/badge-shubun.liquid` を追加。

1. 左サイドバー **「Snippets」** → **「新しいスニペットを追加」**
2. 名前：`badge-shubun`
3. ローカルの `snippets/badge-shubun.liquid` の中身をコピペ
4. 保存

---

## ステップ 7：templates/page.mugicha.json をアップロード

1. **「Templates」** フォルダ → **「新しいテンプレートを追加」**
2. **「ページ用」** を選択
3. テンプレート名：`mugicha`（結果的に `page.mugicha.json` になる）
4. **JSON** 形式を選択
5. デフォルトの中身を消して、`shopify-integration/templates/page.mugicha.json` の中身を全部コピペ
6. 保存

---

## ステップ 8：ページを作成し、テンプレートに割り当て

1. Shopify管理画面 → **オンラインストア** → **ページ** → **ページを追加**
2. **タイトル**：`純手炒り むぎ茶`
3. **URLハンドル**：`mugicha`（→ `/pages/mugicha` になる）
4. 右サイドバー **「テーマテンプレート」** で **「mugicha」** を選択
5. 内容欄は空でOK（テンプレートで全て制御）
6. 保存

---

## ステップ 9：テーマエディタで商品を紐づける

1. **オンラインストア** → **テーマ** → 複製テーマの **「カスタマイズ」**
2. 左上のプルダウンから **「ページ」** → **「純手炒り むぎ茶」** を選択
3. 左サイドバーで **「麦茶ヒーロー」** セクションをクリック
4. **「対象商品」** に、ステップ2で作った麦茶商品を選択
5. **「メイン画像」** に、麦茶の写真を選択
6. 必要に応じて、バリアントの説明文などを編集
7. **右上の「保存」**

---

## ステップ 10：プレビューで確認

1. テーマ一覧に戻る
2. 複製テーマの **「アクション」** → **「プレビュー」**
3. URL の末尾に `/pages/mugicha` を追加してアクセス

**確認ポイント：**
- [ ] ヒーローの左右レイアウトが崩れていない
- [ ] 朱印スタンプが3つ並んでいる
- [ ] バリアントボタンをクリックすると、価格と「税込 / 〜」が切り替わる
- [ ] 数量ボタン（−／＋）が動く
- [ ] 「カートに入れる」で右上にトーストが出て、ヘッダーのカート数が増える
- [ ] 「今すぐ購入する」でチェックアウト画面へ

---

## トラブルシューティング

### Q. 「麦茶ヒーロー」セクションに商品が選べない
→ ステップ2で商品を「有効」にしていない可能性。管理画面で商品ステータスを確認。

### Q. カート追加ボタンを押しても何も起こらない
→ 対象商品が未設定、またはバリアントIDが不正。テーマエディタの「対象商品」を再選択。

### Q. 画像が表示されない
→ Shopify にアップロードした画像を、テーマエディタで「メイン画像」「サムネイル画像」に選び直す必要があります。ローカルの `../assets/photos/` のパスは本番では効きません。

### Q. Google Fontsが遅い
→ 初回アクセス時のみフォントが後読みされます。気になる場合はShopifyにフォントをアップロードして self-host する方法もあります（後日ガイド作成可）。

---

## フェーズ2でやること

フェーズ1でOKが出たら、以下のセクションを順次追加します：

- `sections/mugicha-proof.liquid`（4つの証明ストリップ）
- `sections/mugicha-benefit.liquid`（Why This Mugicha 3列）
- `sections/mugicha-compare.liquid`（一般 vs これ）
- `sections/mugicha-howto.liquid`（飲み方4シーン）
- `sections/mugicha-recipe.liquid`（淹れ方4ステップ）
- `sections/mugicha-maker.liquid`（作り手ストーリー）
- `sections/mugicha-limited.liquid`（量を限る誇り）
- `sections/mugicha-twins.liquid`（上野原の百年つながり）
- `sections/mugicha-lineup.liquid`（5サイズラインナップ）
- `sections/mugicha-faq.liquid`（FAQ）
- `sections/mugicha-specs.liquid`（スペック表）
- `sections/mugicha-final-cta.liquid`（最終CTA）

各セクションは管理画面から追加・順序変更・非表示ができるように作ります。
